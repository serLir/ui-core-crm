UI Core React Modules
for remote installing please perform on command line: 
git config --global url."https://".insteadOf git://

and add to package.json
block dependencies:
"ui-core-modules": "https://github.com/serLir/ui-core-crm.git"
